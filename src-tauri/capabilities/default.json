{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Only what the single window actually needs. Rendering, verification, PDF work and every file write happen in Rust commands, so no filesystem or shell permission is granted here.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:event:default",
    "dialog:allow-save",
    "dialog:allow-open",
    "opener:allow-reveal-item-in-dir",
    "clipboard-manager:allow-write-text",
    "clipboard-manager:allow-read-text"
  ]
}
